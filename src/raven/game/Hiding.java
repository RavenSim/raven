package raven.game;

import raven.game.messaging.Telegram;
import raven.math.Vector2D;
import raven.game.BaseGameEntity;

/*public Vector2D GetHidingPosition(Vector2D posOb){
	double DistanceFromBoundary = 30.0;
	
	double DistAway = radiusOb + distanceFromBoundary;
	
	Vector2D ToOb;
	
	return (ToOb * DistAway) + posOb;
}

public Vector2D Hide(Vehicle target, vector<BaseGameEntity> obstacles){
	double DistToClosest = MaxDouble;
	Vector2D BestHidingSpot;
	
	vector<BaseGameEntity> curOb = obstacles.begin();
	while(curOb != obstacles.end()){
		Vector2D HidingSpot = GetHidingPosition((curOb).Pos(), (curOb).BRadius(), target.Pos());
		
	}
}*/